# Core dependencies for HuggingFace Model Runner
torch>=2.0.0
transformers>=4.30.0
datasets>=2.12.0
sentence-transformers>=2.2.0

# Web UI dependencies
flask>=2.3.0

# Video generation dependencies (AnimateDiff-based TikTok-style video generator)
diffusers>=0.30.0
accelerate>=0.25.0
sentencepiece>=0.1.99
protobuf>=3.20.0
imageio[ffmpeg]>=2.31.0
peft>=0.6.0
numpy>=1.24.0
Pillow>=9.5.0

# Optional: For better performance and package management
# uv>=0.1.0  # Install separately: curl -LsSf https://astral.sh/uv/install.sh | sh
